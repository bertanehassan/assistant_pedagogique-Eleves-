{
  "name": "hassan-bertane-ai",
  "private": true,
  "version": "4.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd vue-app && npm run dev",
    "build": "cd vue-app && npm install && npm run build",
    "preview": "cd vue-app && npm run preview"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "eslint": "^8.57.0"
  }
}
